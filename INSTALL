python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
python3 tnc.py

On first run it will generate a default settings.yaml file

You can edit the file or simply run tnc.py again and configure via the /static/settings.html page (link also on bottom right of map)

To view the web interface, visit:
http://<IP of host>:5001/

